steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'run', 'deploy', 'moneyantra', 
      '--source=.', 
      '--region=us-central1', 
      '--platform=managed', 
      '--allow-unauthenticated'
    ]

timeout: '900s'
